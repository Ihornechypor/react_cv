import styled from 'styled-components';

const Main = styled.main`
  max-width: 900px;
  margin: 0 auto;
  display: grid;
  grid-auto-flow: row;
  grid-template-rows: 0.2fr 0.7fr 0.1fr;
`;

export { Main };
