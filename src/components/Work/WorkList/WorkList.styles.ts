import { styled } from 'styled-components';

const WorkList = styled.ul`
  list-style-type: none;
  padding: 0;
  margin: 0;

  h3 {
    margin-top: 12px;
    font-size: 14px;
  }

  a {
    line-height: 1.4;
  }
`;

export { WorkList };
