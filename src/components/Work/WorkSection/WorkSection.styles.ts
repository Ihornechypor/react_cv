import { styled } from 'styled-components';
import theme from '../../../styles/theme';

const WorkSection = styled.section`
  padding: 5px ${theme.global.gutter} 20px 10px;
`;

export { WorkSection };
